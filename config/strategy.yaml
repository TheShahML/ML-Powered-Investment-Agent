# Strategy configuration
strategy:
  name: "ml_momentum_ranking"
  n_holdings: 15
  rebalance_day: 1 # 0=Monday, 1=Tuesday (for execution)
  signal_day: 0 # Monday (for signals)
  min_hold_weeks: 1
  turnover_threshold: 0.05 # Only replace if score gap > 5%
  benchmark_tickers: ["SPY", "QQQ", "VTI"]



